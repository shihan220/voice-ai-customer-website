[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "node --import tsx backend/index.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
